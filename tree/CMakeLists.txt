project(tree)


set(SOURCE_PATH ${PROJECT_SOURCE_DIR}/*)
file(GLOB CPP_FILES_IN_UTILS "${SOURCE_PATH}*.c**")


message(PROJECT_SOURCE_DIR=${PROJECT_SOURCE_DIR})
message(CPP_FILES_IN_UTILS=${CPP_FILES_IN_UTILS})
add_executable(
    ${PROJECT_NAME}
    ${CPP_FILES_IN_UTILS}
)